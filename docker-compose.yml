services:
  gold-tracker:
    build: .
    container_name: gold-tracker
    restart: unless-stopped
    env_file: .env
    environment:
      - TZ=Asia/Ho_Chi_Minh
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data   # sqlite db + rotating logs persist across restarts
